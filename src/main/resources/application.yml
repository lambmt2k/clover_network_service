spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      show-sql: 'true'
  mail:
    host: smtp.gmail.com
    username: ytbbuilamsonla@gmail.com
    port: '587'
    properties:
      mail:
        smtp:
          starttls:
            enable: 'true'
          auth: 'true'
    password: ohrxnidvjxdjvjwx
  datasource:
    password: postgres
    username: postgres
    initialization-mode: always
    url: jdbc:postgresql://db-clover-network.ck32a9mfyksb.ap-southeast-1.rds.amazonaws.com/db_clover_network
    continue-on-error: 'true'
    initialize: 'true'
redis:
  port: '6379'
  host: localhost
jwt:
  secret: clover
clover:
  server_host: 18.143.172.226:8080
  local_host: localhost:8080

